<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mmi">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".StudentMeetingListActivity"></activity>
        <activity android:name=".StudentClassListActivity" />
        <activity android:name=".Teacher.AttendanceDetailActivity" />
        <activity android:name=".Teacher.AttendanceListActivity" />
        <activity android:name=".Teacher.MeetingListActivity" />
        <activity android:name=".Student.StudentActivity" />
        <activity android:name=".Teacher.ClassListActivity" />
        <activity android:name=".ForgotActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>